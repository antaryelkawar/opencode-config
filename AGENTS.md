## Agent skills

Skills are loaded from `.agents/skills/` and `.opencode/skills/` in the opencode-config repo. The full skill tree is available to all agents — see `skills-lock.json` at the opencode-config root for the complete inventory.

### Domain docs

- **`CONTEXT.md`** at the repo root (parent repo, JobOptimizer)
- **`docs/adr/`** — Architecture Decision Records
- **`docs/agents/domain.md`** — detailed domain doc layout

### Issue tracker

GitHub issues in the JobOptimizer parent repository. See `docs/agents/issue-tracker.md` for conventions and CLI commands.

### Triage labels

Canonical labels used for triage roles. See `docs/agents/triage-labels.md` for the label mapping table.
